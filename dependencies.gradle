dependencies {
    compileOnly("org.jetbrains:annotations:26.0.1")
    annotationProcessor('org.spongepowered:mixin:0.8.5:processor')

    // Formula Loader - compile against the deobfuscated local artifact.
    // The obfuscated artifact is embedded by the jarJar task for release builds.
    modImplementation("oftenoviour:Formula Loader:1.20.1-${formula_loader_version}-dev")
    jarJar(name: "Formula Loader", version: "1.20.1-${formula_loader_version}-obf", ext: "jar") { transitive = false }
    modImplementation(name: "gtceu-1.20.1-7.5.0-ctnh-dev")

    // would ideally use API for this, but that does not work well with fg.deobf in my understanding
    modImplementation("slimeknights.mantle:Mantle:${minecraft_version}-${mantle_version}")

    // Compile and run against the optional EMI Forge integration.
    modCompileOnly("curse.maven:emi-580555:8081375")
    modRuntimeOnly("curse.maven:emi-580555:8081375")

    // Keep the original JEI integration available alongside EMI.
    modCompileOnly("mezz.jei:jei-${minecraft_version}-common-api:${jei_version}")
    modCompileOnly("mezz.jei:jei-${minecraft_version}-forge-api:${jei_version}")
    //modRuntimeOnly("mezz.jei:jei-${minecraft_version}-forge:${jei_version}")

    // modImplementation("mcjty.theoneprobe:TheOneProbe-${minecraft_base_version}:${minecraft_base_version}-${probe_version}")

    modCompileOnly("blusunrize.immersiveengineering:ImmersiveEngineering:${minecraft_version}-${ie_version}")
    modRuntimeOnly("curse.maven:MoonlightLib-499980:6000197")

    modCompileOnly("curse.maven:diet-443570:4813904")

    //modImplementation("com.gregtechceu.gtceu:gtceu-${minecraft_version}:${gtceu_version}:slim") { transitive = false }
    modImplementation("com.lowdragmc.ldlib:ldlib-forge-${minecraft_version}:${ldlib_version}") { transitive = false }
    modImplementation("com.tterrag.registrate:Registrate:${registrate_version}")
    modImplementation("dev.toma.configuration:configuration-forge-${minecraft_version}:${configuration_version}")
    modImplementation("curse.maven:MmmMmmMmmMmm-225738:5737040")

    modCompileOnly("dev.gigaherz.jsonthings:JsonThings-${minecraft_version}:${json_things_version}")

    modCompileOnly("curse.maven:balm-531761:5999476")
    modCompileOnly("curse.maven:crafting-tweaks-233071:5623639")
    modCompileOnly("curse.maven:apotheosis-313970:${apotheosis_file_id}")
    modRuntimeOnly("curse.maven:apotheosis-313970:${apotheosis_file_id}")
    modCompileOnly("dev.shadowsoffire:Placebo:${minecraft_version}-${placebo_version}")
    modRuntimeOnly("dev.shadowsoffire:Placebo:${minecraft_version}-${placebo_version}")
    modCompileOnly("dev.shadowsoffire:ApothicAttributes:${minecraft_version}-${apothic_attributes_version}")
    modRuntimeOnly("dev.shadowsoffire:ApothicAttributes:${minecraft_version}-${apothic_attributes_version}")

    //modImplementation("dev.latvian.mods:kubejs-forge:${kubejs_version}")
    //modImplementation("dev.latvian.mods:rhino-forge:${rhino_version}")
    //modImplementation("dev.architectury:architectury-forge:${architectury_version}")

    modCompileOnly("com.simibubi.create:create-${minecraft_version}:${create_version}:slim")
    modCompileOnly("dev.engine-room.flywheel:flywheel-forge-${minecraft_version}:${flywheel_version}")
    modCompileOnly("net.createmod.ponder:Ponder-Forge-${minecraft_version}:${ponder_version}")
    modCompileOnly("vazkii.botania:Botania:${botania_version}")
    modImplementation("com.simibubi.create:create-${minecraft_version}:${create_version}:slim")
    modImplementation("dev.engine-room.flywheel:flywheel-forge-${minecraft_version}:${flywheel_version}")
    modImplementation("net.createmod.ponder:Ponder-Forge-${minecraft_version}:${ponder_version}")
    modImplementation("vazkii.botania:Botania:${botania_version}")

    modImplementation("curse.maven:curios-309927:6418456")
    modImplementation("curse.maven:patchouli-306770:6164575")
    //deployerJars 'org.apache.maven.wagon:wagon-ssh:2.2'

    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.8.2'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.8.2'
    testImplementation 'org.mockito:mockito-inline:4.2.0'
    testImplementation 'org.assertj:assertj-core:3.21.0'
    testRuntimeOnly("vazkii.botania:Botania:${botania_version}") { transitive = false }
}
